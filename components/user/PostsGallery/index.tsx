import { Dialog, DialogContent } from "@/components/shared/ui/dialog";

import * as React from "react";

export function PostGallery() {
	return (
		<Dialog open={true}>
			<DialogContent className="sm:max-w-[425px]"> ss</DialogContent>
		</Dialog>
	);
}
